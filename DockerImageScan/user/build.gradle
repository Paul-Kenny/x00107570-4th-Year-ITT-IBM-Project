buildscript {
    repositories {
        mavenCentral()
        maven {
            url uri('../repo')
        }
    }
    dependencies {
        classpath group:'com.scanImage', name: 'runScanImage',
                version: '1.0-SNAPSHOT'
    }
}
apply plugin: 'scanImage'

repositories {
    mavenCentral()
}
